---
let { id = 'carousel', slides, currentStep = 1 } = Astro.props;

const n_steps = slides?.length;

import Stepper from './Stepper.astro';
---

<div class="carousel" id={id} data-current={currentStep}>
  <slot />

  {
    n_steps && (
      <Stepper
        containerId={id}
        n_steps={n_steps}
        currentStep={1}
        style={'--max-width: var(--narrow-content-width)'}
      />
    )
  }
</div>

<style></style>
