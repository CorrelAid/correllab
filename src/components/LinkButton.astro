---
const { href, variant = '', classes = '', children } = Astro.props;
---

<a
  class="btn"
  href={href}
  class:list={[variant, classes]}
  data-astro-prefetch={href?.startsWith('/') ? 'hover' : null}><slot /> {href?.startsWith('/')}</a
>

<style></style>
